require_relative '../test_helper'

class FormBuilderTest < ActionView::TestCase

  def test_something

    # page = comfy_cms_pages(:default)
    # builder = ComfortableMexicanSofa::FormBuilder.new(:page, page, self, {})

    # page.layout.content = '{{ cms:page:content:check_box({"icon": "user", "placeholder": "test@example.org"})  }}'
    # p builder.page_tag(page.tags(true).last, 1)

    # page.layout.content = '{{ cms:page:content:select( ["VISA", "MasterCard"] ) }}'
    # p builder.page_tag(page.tags(true).last, 1)


  end

end
